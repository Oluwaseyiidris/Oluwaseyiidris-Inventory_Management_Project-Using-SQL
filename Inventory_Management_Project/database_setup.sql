# Set up the database, choose schema, and list tables available.

-- Select the database and set up schema
SELECT DATABASE();
USE new_schema;

-- Show all available tables in the database
SHOW tables;
